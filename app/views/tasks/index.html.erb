<div align="center">

  <marquee class="field">Welcome to Categories</marquee>	
  <b class="field">Categories:</b> 
	<% Category.all.each do |category| %>
	<%= link_to "#{category.name}", tasks_path(category: category.name)%>
  <%end%>
</div>  
